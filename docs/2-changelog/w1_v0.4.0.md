# Changelog - Week 1, 02-05-2026, V. 0.4.0

**Release Date**: Week 1, 02-05-2026 at 17:19
**Version**: 0.4.0 (previously 0.3.1)
**Object**: Decouple overlap diagnostic from pipeline, add on-demand validation
**Code review**: `docs/3-code-review/CR_w1_v0.4.0.md` (Codex loop, 4 rounds, APPROVED)

## Changes

- Decoupled the slow O(N²) volume-intersection classification from the mandatory STEP→Embree pipeline. The pipeline now calls fast `extract_contacts()` (triangle-match tied pairing + mismatched-contact detection) instead of `diagnose_overlaps()`.
- `ValidatedAssembly` gains `contacts: ContactReport` (always populated); `overlaps: OverlapReport` is now optional (`None` by default).
- Adapter gate split: mismatched-contact gate (always active) + interference gate (only when full validation has been run).
- New `raysim validate` CLI subcommand: runs the full overlap diagnostic on a STEP file with exit codes (0 = clean, 1 = issues).
- New "Validate Geometry" button in the Run panel: runs `diagnose_overlaps` in a `ValidateWorker` QThread (off the GUI thread).
- Validation state tracked via `AppState.overlap_validated` with monotonic `geometry_revision` counter to prevent stale async results.
- Human-metadata `.human.json` sibling file now written alongside `run.json`, recording `overlap_validation` status and `overlap_report_summary` counts.
- 3 new `extract_contacts` unit tests in `test_overlap.py`.
